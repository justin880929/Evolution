import { CourseProductPipe } from './course-product.pipe';

describe('CourseProductPipe', () => {
  it('create an instance', () => {
    const pipe = new CourseProductPipe();
    expect(pipe).toBeTruthy();
  });
});
